<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="T SURENDRA & ASSOCIATES, Chartered Accountants - Audit, consulting, financial advisory, tax services." />
  <meta name="author" content="T SURENDRA & ASSOCIATES" />
  <title>T SURENDRA & ASSOCIATES - Chartered Accountants</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --blue: #082567;
      --green: #9ACD32;
      --pale-green: #f0faea;
      --light-gray: #f5f6fa;
      --light-blue: #e7f0fa;
      --white: #FFFFFF;
    }
    /* your existing CSS here (no changes, included in original code) */

    /* === Additional styles for loader animation === */
    #loader-overlay {
      position: fixed;
      left: 0; top: 0;
      width: 100vw; height: 100vh;
      background: var(--blue);
      z-index: 99999;
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 1;
      visibility: visible;
      transition: opacity 0.5s ease, visibility 0.5s ease;
    }
    #loader-overlay.hidden {
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
    }
    .dot-loader {
      display: flex;
      gap: 18px;
    }
    .dot-loader div {
      width: 18px; height: 18px;
      border-radius: 50%;
      background: var(--green);
      opacity: 0.75;
      animation: dotPulse 1.15s infinite cubic-bezier(.36,0,.64,1.07);
    }
    .dot-loader div:nth-child(2) {
      animation-delay: 0.23s;
      background: #21b3ff;
    }
    .dot-loader div:nth-child(3) {
      animation-delay: 0.46s;
      background: #5e86ff;
    }
    @keyframes dotPulse {
      0%, 100% { opacity: 0.72; transform: scale(1); }
      25% { opacity: 1; transform: scale(1.23); }
      50% { opacity: 0.7; transform: scale(0.94); }
      75% { opacity: 0.9; transform: scale(1.1); }
    }
    body.loading > *:not(#loader-overlay) {
      display: none !important;
    }
    body.loading {
      overflow: hidden !important;
    }

    /* === Styles for Startup Advisory content overlay === */
    #startup-advisory-content {
      display: none;
      max-width: 1100px;
      margin: 40px auto;
      padding: 24px;
      background: var(--white);
      border-radius: 16px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.11);
      color: var(--blue);
      position: relative;
      z-index: 5000;
    }
    #startup-advisory-content button#start-advisory-close {
      position: absolute;
      top: 14px;
      right: 22px;
      background: var(--green);
      color: var(--blue);
      border: none;
      padding: 8px 20px;
      border-radius: 6px;
      font-weight: 600;
      cursor: pointer;
      font-size: 1.15rem;
      transition: background 0.16s ease;
    }
    #startup-advisory-content button#start-advisory-close:hover,
    #startup-advisory-content button#start-advisory-close:focus {
      background: var(--blue);
      color: var(--white);
      outline: none;
    }
  </style>
</head>
<body class="loading">
  <!-- Loader Overlay -->
  <div id="loader-overlay" aria-label="Loading">
    <div class="dot-loader">
      <div></div><div></div><div></div>
    </div>
  </div>

  <!-- Your existing content follows unchanged -->
  <!-- ... header, nav, carousel, services, team, contact, footer as per your original code -->

  <!-- Existing code copied below with needed slight id addition on second advisory link -->
  <header>
    <div class="container">
      <nav>
        <a href="#top" class="logo" aria-label="T Surendra & Associates Logo">
          <img src="https://github.com/raghava239/tsurendraca/blob/main/logo.jpg?raw=true" alt="T Surendra & Associates Logo" />
        </a>
        <ul id="nav-menu">
          <li><a href="#top" class="active">Home</a></li>
          <li><a href="#top">About</a></li>
          <li class="dropdown">
            <a href="#services" class="dropdown-toggle" aria-haspopup="true" aria-expanded="false">Services</a>
            <ul class="dropdown-menu" role="menu" aria-label="Services Submenu">
              <li><a href="#" id="startup-advisory-link" data-service="startup-advisory">Start-up Advisory and Consulting Services</a></li>
              <li><a href="virtual-cfo.html" data-service="virtual-cfo">Virtual CFO Services</a></li>
              <li><a href="direct-international-tax.html" data-service="direct-international-tax">Direct and International Tax Services</a></li>
              <li><a href="indirect-tax.html" data-service="indirect-tax">Indirect Tax Services</a></li>
              <li><a href="audit-assurance.html" data-service="audit-assurance">Audit and Assurance Services</a></li>
              <li><a href="labour-law-compliance.html" data-service="labour-law-compliance">Labour Law Compliance Services</a></li>
              <li><a href="secretarial-fema-rbi.html" data-service="secretarial-fema-rbi">Secretarial, FEMA & RBI Compliance Services</a></li>
            </ul>
          </li>
          <li><a href="#team-section">Team</a></li>
          <li><a href="#contact">Contact</a></li>
          <li><a href="#contact" class="btn-primary">Message Us</a></li>
        </ul>
        <div class="menu-toggle" aria-label="Toggle menu" tabindex="0" role="button">
          <span></span><span></span><span></span>
        </div>
      </nav>
    </div>
  </header>
  <!-- The rest of your code for carousel, services, team, contact, footer remains unchanged -->

  <!-- Add unique id on the services section advisory link to also bind -->
  <section id="services" aria-label="Services">
    <div class="services-container">
      <div class="image-block" aria-hidden="true">
        <img src="https://github.com/raghava239/tsurendraca/blob/main/logo.jpg?raw=true" alt="Services Image" />
      </div>
      <div class="text-block">
        <ul>
          <li><a href="#" id="startup-advisory-link-alt" data-service="startup-advisory">Start-up Advisory and Consulting Services</a></li>
          <li><a href="virtual-cfo.html">Virtual CFO Services</a></li>
          <li><a href="direct-international-tax.html">Direct and International Tax Services</a></li>
          <li><a href="indirect-tax.html">Indirect Tax Services</a></li>
          <li><a href="audit-assurance.html">Audit and Assurance Services</a></li>
          <li><a href="labour-law-compliance.html">Labour Law Compliance Services</a></li>
          <li><a href="secretarial-fema-rbi.html">Secretarial, FEMA & RBI Compliance Services</a></li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Add Startup Advisory content overlay container -->
  <section id="startup-advisory-content" aria-live="polite" aria-atomic="true"></section>

  <!-- Your full other content goes here unchanged -->

  <footer>
    <p>T SURENDRA & ASSOCIATES. All Rights Reserved</p>
  </footer>

  <script>
    // Loader: fade out without white flash
    window.addEventListener('DOMContentLoaded', function() {
      setTimeout(function () {
        const loader = document.getElementById('loader-overlay');
        loader.classList.add('hidden');
        setTimeout(() => {
          loader.style.display = 'none';
          document.body.classList.remove('loading');
        }, 500);
      }, 1500);
    });

    // Hamburger toggle
    const menuToggle = document.querySelector('.menu-toggle');
    const navMenu = document.getElementById('nav-menu');
    menuToggle.addEventListener('click', () => {
      navMenu.classList.toggle('active');
    });
    navMenu.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        navMenu.classList.remove('active');
      });
    });

    // Mobile dropdown toggle
    document.querySelectorAll('nav ul li.dropdown > a').forEach(toggle => {
      toggle.addEventListener('click', e => {
        if(window.innerWidth <= 991) {
          e.preventDefault();
          const parentLi = e.target.closest('li.dropdown');
          parentLi.classList.toggle('active');
        }
      });
    });

    // Carousel initialization etc. (No changes)

    // Contact form validation (No changes)

    // Load Startup Advisory content inline from external file on click
    function loadStartupAdvisory(e) {
      e.preventDefault();
      const container = document.getElementById('startup-advisory-content');
      if(container.style.display === 'block') {
        container.scrollIntoView({behavior: 'smooth'});
        return;
      }
      container.style.display = 'block';
      container.innerHTML = '<p>Loading content...</p>';
      fetch('startup-advisory.html')
        .then(res => {
          if (!res.ok) throw new Error('Failed to load advisory content.');
          return res.text();
        })
        .then(html => {
          const tempDiv = document.createElement('div');
          tempDiv.innerHTML = html;
          // Attempt to extract main content only (change selector if different)
          let mainContent = tempDiv.querySelector('#main, .main, main')?.innerHTML ?? tempDiv.body.innerHTML ?? html;
          container.innerHTML = `<button id="start-advisory-close" aria-label="Close advisory section">Close ×</button>` + mainContent;
          container.scrollIntoView({behavior: 'smooth'});
          document.getElementById('start-advisory-close').onclick = () => {
            container.style.display = 'none';
          };
        })
        .catch(err => {
          container.innerHTML = `<p style="color:red;">Error loading advisory content: ${err.message}</p>`;
        });
    }

    document.getElementById('startup-advisory-link').addEventListener('click', loadStartupAdvisory);
    document.getElementById('startup-advisory-link-alt').addEventListener('click', loadStartupAdvisory);
  </script>
</body>
</html>
